import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
    palette: {
        primary: {
            main: '#764abc',
            secondary: '#1fe628'
        }
    },
    background: {
        deafult: '#fff'
    }
})

export default theme